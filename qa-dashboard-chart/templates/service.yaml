apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa-dashboard-chart.fullname" . }}
  labels:
    {{- include "qa-dashboard-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qa-dashboard-chart.selectorLabels" . | nindent 4 }}
