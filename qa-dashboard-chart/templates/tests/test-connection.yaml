apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qa-dashboard-chart.fullname" . }}-test-connection"
  labels:
    {{- include "qa-dashboard-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qa-dashboard-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
